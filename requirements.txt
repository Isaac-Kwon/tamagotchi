# Soul Tamagotchi — runtime dependencies
# The agent core depends only on httpx + the Python standard library.
# fastapi / uvicorn / mcp are listed for later milestones (web API, MCP server).
httpx
fastapi
uvicorn
# FastAPI multipart form parsing — the outbox resolve endpoint accepts
# multipart/form-data (status/note fields + an optional file upload).
python-multipart
mcp
# IANA timezone database — zoneinfo has no bundled DB on Windows (spec P5:
# report scheduling uses Asia/Seoul via zoneinfo).
tzdata

# Dev dependency (not required at runtime): pytest
# Install with:  pip install pytest   (or see requirements-dev.txt)
