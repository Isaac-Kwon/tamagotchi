# Local configuration (secrets) — commit config.example.json instead
config.json

# Agent data directory (has its own git repo)
data/

# Python virtual environment
.venv/

# Python caches
__pycache__/
*.pyc
.pytest_cache/

# Runtime-generated placeholder web client (the real UI is built separately).
# create_app() writes soul/web/static/index.html only when the dir is empty.
soul/web/static/
