# Local configuration (secrets) — commit config.example.json instead
config.json

# Agent data directory (has its own git repo)
data/

# Python virtual environments (Windows / WSL)
.venv/
.venv-wsl/

# Python caches
__pycache__/
*.pyc
.pytest_cache/
